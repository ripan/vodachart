require 'spec_helper'

describe "ProductFamilies" do
  describe "GET /product_families" do
    it "works! (now write some real specs)" do
      get product_families_path
      expect(response.status).to be(200)
    end
  end
end
